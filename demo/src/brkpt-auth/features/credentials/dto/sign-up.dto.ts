/**
 * Demo only. In your project, define your own fields.
 * The corresponding dto in lib/ is intentionally empty.
 */
export class SignUpDto {
  name!: string;
  email!: string;
  password!: string;
  confirmedPassword!: string;
}
